
ctl.equal {
	type equal;
}

pcm.microphone {
	card 0
	device 0
	subdevice 0
}

pcm.equalizer {
	type equal;
	slave.pcm "plug:dmix";
}

pcm.!default {
	type asym;
	playback.pcm {
		type plug;
		slave.pcm "equalizer";
	}
	capture.pcm {
		type plug;
		slave.pcm "microphone";
	}
}
